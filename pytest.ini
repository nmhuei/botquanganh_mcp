[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra
markers =
    strength: stress and robustness probes that hammer real machinery (capacity, executor, RPC surface)
    gui: real Tk widget verification; run under a graphical display or xvfb-run
